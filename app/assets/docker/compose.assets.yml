services:
  ptekwpdev-assets:
    container_name: ${ASSETS_CONTAINER}
    build:
      context: .
      dockerfile: Dockerfile
    command: tail -f /dev/null   # 👈 keeps the container running
    volumes:
      - ${ASSETS_VOLUME}:${ASSETS_ROOT}   # 👈 named volume 
    restart: unless-stopped

volumes:
  ptekwpdev_assets_volume:   # 👈 declare the volume
    name: ${ASSETS_VOLUME}